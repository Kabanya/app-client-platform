name: app_account
description: Account, entitlement, quota, and sync contracts for the app suite.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  http: ^1.2.2
  sign_in_with_apple: ^8.1.0
  supabase_flutter: ^2.16.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
